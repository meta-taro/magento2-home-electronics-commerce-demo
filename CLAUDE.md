# CLAUDE.md — Claude Code 作業ルール

このファイルは、Claude Code が本リポジトリで作業するときの基本ルールです。
作業前に必ず目を通してください。

## 1. このリポジトリの目的

- Magento Open Source を使った **学習・検証用の公開デモ**リポジトリです。
- 架空の家電メーカー「NOVA HOME Electronics」の海外向けECサイトを題材に、Adobe Commerce / Magento案件で頻出する技術要素(テーマ開発、モジュール開発、DI、Plugin、Observer、routes.xml、events.xml、REST API、GraphQL、外部システム連携、商品・在庫・注文同期)を手を動かして理解することが目的です。
- **商用案件のソースコードではありません。** 本番運用を前提とした実装は不要です。

## 2. 作業時の基本方針

- いきなりコードを書き始めない。**実装前に、対象ファイルと変更方針をユーザーに確認する。**
- 大きな変更は Phase(`docs/ROADMAP.md`)に沿って小さく分割する。
- 1ファイルずつ、差分が追える単位で進める。
- 不明点は推測で埋めず、ユーザーに質問するか、公式ドキュメントを確認する。
- 既存ファイルを上書き・削除する前に内容を確認し、想定と違えばユーザーに報告する。

## 3. Magento / Adobe Commerce 学習用であること

- 学習目的なので、「なぜそうするのか」をコメントや `docs/LEARNING_LOG.md` で説明する。
- Magento の標準的なディレクトリ構成・命名規約(`app/code/<Vendor>/<Module>/`、`app/design/frontend/<Vendor>/<theme>/` 等)に従う。
- フレームワークの作法を逸脱した「動けばよい」実装はしない。

## 4. 公式ドキュメントを優先する

- 一次情報は **Adobe Commerce / Magento の公式ドキュメント(DevDocs)** とする。
- ライブラリ・コンポーネントの使い方は、公式 docs または Context7 で確認してから実装する。
- ブログ記事等の二次情報は補助として使い、最終判断は公式に合わせる。
- バージョン差異(2.4.x 系のマイナーバージョン)に注意する。

## 5. 推測で危険な実装をしない

- DB スキーマ変更、`setup:upgrade`、キャッシュ全削除、`bin/magento` の破壊的コマンドなどは、実行前に必ず内容と影響を説明し、ユーザーの承認を得る。
- 本番想定のデータを壊す操作はしない。デモ・サンプルデータの範囲で行う。
- 自信のない API・設定は、まず小さく試して動作を確認する。

## 6. 実装前の確認フロー

1. 何を実装するか(目的)を一文で述べる
2. 触る対象ファイル一覧を出す
3. 変更方針(追加 / 修正 / 削除と、その理由)を述べる
4. ユーザーの承認を待つ
5. 実装する
6. `git diff` で差分を確認する
7. 動作確認する
8. `docs/LEARNING_LOG.md` にログを残す

## 7. 学習ログを必ず残す

- コードや設定を変更したら、`docs/LEARNING_LOG.md` のテンプレートに沿って記録する。
- 記録項目: Date / Topic / Goal / Files changed / What was implemented / What was verified / Errors / Issues / Fixes / Notes(学び・気づき)。
- 大きな設計判断は `docs/ARCHITECTURE.md` にも追記する。

## 8. 秘密情報をコミットしない

- APIキー、トークン、パスワード、`.env` の実値、`auth.json`(Magento Marketplace 認証)などは **絶対にコミットしない**。
- 連携のサンプルはダミー値・スタブで作る。実値が必要な場合は `.env.example` 形式で雛形のみ置く。
- 既存ファイルにシークレットらしき文字列を見つけたら、コミット前にユーザーへ報告する。

## 9. 商標・実在メーカー名を使わない

- 実在の家電メーカー名・製品名・商標を使わない。
- メーカー名は架空の **"NOVA HOME Electronics"** を使う。Vendor 名(モジュール名前空間)も架空のもの(例: `NovaHome`)を使う。
- ロゴ・画像も実在ブランドのものを使わない。

## 10. AI生成コードをそのまま信用しない

- AI が生成したコードは下書きとして扱う。
- 必ず: 動作確認(`bin/magento` / ブラウザ / API クライアント)、差分確認(`git diff`)、ログ確認(`var/log/` 等)を行う。
- テストが書ける箇所はテストを書く。
- 「たぶん動く」で終わらせず、確認した内容を `docs/LEARNING_LOG.md` の "What was verified" に書く。

## 11. Windows / PowerShell 環境を優先

- 開発ホストは Windows / PowerShell 想定。コマンド例は PowerShell で動くものを優先する(`$null`、`$env:VAR`、バッククォート継続など)。
- Magento 本体は Docker / WSL2 / Linux コンテナ内で動かす想定。ホスト操作(PowerShell)とコンテナ内操作(bash)を区別して書く。
- パス区切りや改行コード(CRLF/LF)に注意する。

## 12. やってよいこと / 確認が必要なこと

| 区分 | 例 |
|------|----|
| 確認なしで可 | ドキュメント追記・修正、ログ記録、読み取り専用の調査(`Read` / `Grep` / `git status` / `git diff`) |
| 事前確認が必要 | ファイル新規作成、コード変更、`composer` 操作、`bin/magento` 実行、Docker 構成変更、依存追加 |
| 原則やらない | 破壊的コマンド、強制 push、シークレットのコミット、実在ブランド名の使用、本番想定データの破壊 |
